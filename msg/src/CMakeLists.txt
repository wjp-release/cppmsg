aux_source_directory(. cur_srcs)
aux_source_directory(./neuron neuron_srcs)
aux_source_directory(./posix/tcp posixtcp_srcs)
aux_source_directory(./posix/uds posixuds_srcs)
aux_source_directory(./posix posix_srcs)
aux_source_directory(./endpoint endpoint_srcs)
add_library(msg ${posixuds_srcs} ${posix_srcs} ${endpoint_srcs} ${neuron_srcs} ${cur_srcs} ${posixtcp_srcs})  
if(UNIX)
	target_link_libraries(msg pthread)
endif()
